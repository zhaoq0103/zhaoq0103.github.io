# 创建并激活虚拟环境依赖，避免系统污染）
python3 -m venv myenv
source myenv/bin/activate  # macOS/Linux
# myenv\Scripts\activate  # Windows

# 在虚拟环境中安装 requests
......  handle your task

deactivate  # 通用命令，适用于所有系统[1,3,7](@ref)